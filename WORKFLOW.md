# 工作流規格

## 目標

以有限、可追溯的跨工具審查，產生一份供人決策的方案；在授權範圍內執行後，依明確驗收條件記錄結果。

這個流程不把語言模型視為彼此獨立的真相來源，也不以多數決代替證據。

## 1. 開始前：定義任務邊界

先記錄以下項目：

- 任務負責人和任務識別碼。
- 要回答的問題，以及預期交付物。
- 允許使用的材料、資料來源和工具。
- 不得執行的行動、不得外傳的資料。
- 最多完整討論循環數、最多模型請求數與完成條件。
- 若計畫進入執行，允許的檔案、服務、資料和副作用範圍。
- 執行結果如何驗收，以及哪些情況應停止。

把「討論到哪裡」和「允許做到哪裡」分開描述。若沒有明確執行授權或既有授權不涵蓋某個動作，流程停在供決策的候選方案。

## 2. 凍結輸入材料

建立一份每位參與者都能取得的材料包：

1. 整理任務說明、來源資料和預期輸出格式。
2. 保存唯讀原件或明確版本，計算 SHA-256；若材料太大無法直接提供，列出檔名、版本、雜湊與可讀取方法。
3. 把要審查的內容放在明確邊界內，避免 shell 插值或格式處理改寫材料。
4. 每次新增、刪除或修訂材料，都建立新版本並記錄差異；不可默默沿用舊雜湊。
5. 登記實際傳給每個模型的輸入雜湊或逐字內容，確認沒有截斷或漏載。

若某參與者無法讀取材料，結果標為 INPUT_UNAVAILABLE。只有在資料權限允許時，才改用經使用者核准的貼上方式；不可把未讀到的材料描述成已審查。

## 3. 送出前檢查

每個工具呼叫前，記錄：

- 工具與介面，例如 ChatGPT Pro 網頁介面或 Claude Code CLI。
- 介面實際顯示的模型名稱與可見推理設定；看不到就記 UNKNOWN。
- 身分驗證狀態與來源。只記錄必要的狀態，不保存 Token、Cookie 或祕密。
- 本次模型請求序號、任務雜湊、輸入雜湊和提示詞版本。
- 此角色要回答的問題，以及禁止執行的動作。

使用者登入的訂閱介面和 CLI 可能不是穩定 API；登入成功、模型標籤顯示或工具回報完成，都不能取代對實際內容與來源的核對。不同產品介面也不證明底層模型或資料來源彼此獨立；除非有可查證的依據，否則只記錄各工具實際顯示的模型資訊。

## 4. 有限討論循環

開始前分別設定最多完整討論循環數和最多模型請求數，並約定計數方式；整個任務維持一致：

- **模型請求**：一次實際送出的請求。每次重試都是新的模型請求，必須計數和留下原因。
- **討論循環**：一次完整的 Pro 初步分析、Claude 實質審查、Pro 針對意見回應。
- **最終確認**：若要宣稱雙方形成共識，需另外取得各方明確確認；只有協調者總結或最後一方沉默，不算共識。

第一個循環由 Pro 提出初步分析；後續循環則從最新候選方案與尚未解決的意見開始。每個循環都完成以下三步：

1. **Pro 初步分析**：回答任務問題、列出主張、證據、假設、風險和待確認事項。
2. **Claude 對抗性審查**：使用相同的凍結材料，逐項指出錯誤、缺漏、不可驗證的推論和可能的失效情境；不要要求它無條件贊成。
3. **Pro 回應**：逐項標記採納、部分採納、拒絕或未知，並說明依據。
4. **循環結束檢查**：確認每個步驟都收到可讀且實質的內容；如有缺漏，該循環標為 INCOMPLETE 或 BLOCKED，不計為完整循環。
5. **停止檢查**：達到預定次數、遇到超時、輸入不完整、身分無法確認或出現超出範圍的行動時，停止並保留現況。沒有核准時不得自動重試、換模型或轉用付費 API。

回覆內容必須實際讀回並檢查。工具的 terminal status 為 completed，只表示呼叫終止，不代表內容完整、正確或符合要求。

## 5. 整理供決策的候選方案

協調者整理：

- 建議方案和明確的依據。
- 每位參與者實際提出的關鍵主張。
- 已採納、部分採納、拒絕和未解決的意見。
- 事實、推論、假設和未知項的區分。
- 尚缺的材料、驗證工作、風險和停止條件。
- 是否符合任務原定驗收條件。
- 審查循環及實際模型請求數。

除非所有必要參與者都清楚確認，否則狀態使用 CANDIDATE_FOR_DECISION；不要寫「共識」、「確認正確」或「已驗證」。

## 6. 執行關卡

進入執行前，核對：

- 執行計畫與討論後的候選方案一致，並引用其版本或雜湊。
- 每一項預期變更、影響範圍和驗收方式都已列明。
- 執行權限來自明確的使用者指示，或先前已授權且涵蓋該動作的範圍。
- 任何超出授權的外部發布、資料刪除、付款、權限提升或不可逆動作，都先停下來取得相應決定。
- 執行工具可用且已核對，材料和輸出位置正確。

執行前記錄基線；執行後逐項記錄實際結果。對每個項目使用 PASS、FAIL、BLOCKED 或 NOT_ATTEMPTED，並附上可核對的證據。不要因為執行器回報成功，就跳過結果驗收。

## 7. 狀態與失敗處理

| 狀態 | 意義 |
| --- | --- |
| DRAFT | 任務範圍尚未凍結。 |
| PREPARED | 範圍、材料包、計數方式和驗收條件已記錄。 |
| PREFLIGHT_PASSED | 必要介面、登入狀態和輸入可用性已檢查。 |
| REVIEWING | 有限回合審查進行中。 |
| INCOMPLETE | 有模型請求未取得完整或可讀的回覆。 |
| BLOCKED | 權限、材料、身分、工具或其他必要條件不足。 |
| CANDIDATE_FOR_DECISION | 候選方案已整理，尚未由決策者採用或授權執行。 |
| EXECUTION_AUTHORIZED | 具有涵蓋本次工作的明確授權。 |
| EXECUTING | 執行已開始，結果尚未完成核對。 |
| VERIFIED | 所有必要驗收條件均有證據通過。 |
| PARTIAL | 有項目完成，但至少一項必要結果未完成或未驗證。 |
| CANCELLED | 決策者取消後續工作。 |

狀態只能在相應證據存在時前進。未知結果不得推定為通過；在不確定是否已執行的情況下，先核對副作用和收據，再決定是否重試。

## 8. 完成紀錄

每次任務至少保留：

- 凍結的任務說明與材料包版本／雜湊。
- 每位參與者的工具、模型標籤、必要登入檢查與時間。
- 每次模型請求的輸入、輸出、結果狀態和內容雜湊。
- 循環計數、重試原因、分歧處理和未解決項目。
- 決策或執行授權的來源與時間。
- 執行清單、逐項結果、驗收證據和最終狀態。

公開分享時，先移除本機路徑、帳號識別資料、秘密、私人研究材料和未獲授權的輸出。
