# 流程規格：Luna 協調的計畫交叉檢核

[English](WORKFLOW.en.md)

## 目的與設計理由

使用 GPT-6 Luna 作為 Codex 協調者，讓它負責需求轉送、往返溝通、計數和紀錄；由側欄 Pro（模型介面顯示 GPT-6）提出計畫；再由 Claude Code 和 Agent Teams 挑戰與檢核計畫，最後由 Pro 逐項回應。

同一模型同時規劃和審查，可能沿用同一套假設或漏看同一類問題。因此，本流程安排其他審查角色來尋找不同意見。這是一種降低共享盲點風險的設計，不保證審查者完全獨立，也不保證結果無誤。採用 Luna 作協調者的目標是控制協調成本；實際節省需另行量測。

## 1. 角色和責任

| 角色 | 責任 |
| --- | --- |
| 使用者／決策者 | 提出需求、定義限制與成功條件，並授權或拒絕執行。 |
| Codex 協調者（GPT-6 Luna） | 傳遞需求與計畫、維護唯一版本、執行循環上限、記錄審查並整理待決項。 |
| 側欄 Pro（介面標示 GPT-6） | 建立初始計畫，逐項處理審查意見並提供修訂版。 |
| Claude Code | 在 Pro 對話之外扮演交叉檢核角色；指出缺漏、風險、反例和需驗證主張。 |
| Agent Teams | 讓多個代理討論計畫和審查意見；記錄各代理可見輸出、團隊結論與模型資訊。 |

「另一個工具」不一定等於「統計上獨立的模型」。若 Agent Teams 的成員模型或內部呼叫數無法從介面確認，明確記為 UNKNOWN 或僅知聚合結果。

## 2. 開始前凍結任務與材料

記錄目標、交付物、允許／禁止事項、成功條件、執行授權範圍，以及最多完整循環數和最多模型請求數。

整理唯讀材料包，記錄版本與 SHA-256。每個角色實際收到什麼、是否截斷或無法讀取，都要記下來。材料更新就建立新版本和新雜湊。未讀到的檔案不可描述成已審查。

## 3. 預檢工具與模型

每次呼叫前記錄角色、工具、介面顯示的模型名稱、可見推理設定、輸入版本、提示詞版本和請求序號。只保留必要的登入狀態，不記錄密碼、Token、Cookie 或授權標頭。

Pro 側欄的模型標籤、Codex 協調者選用的 Luna，以及 Claude Code 或 Agent Teams 的模型設定都依當次可見資訊記錄；看不到就標 UNKNOWN，不由名稱推測後端。工具回報 completed 只代表呼叫結束，仍須讀回內容。

## 4. 計數定義

開始前設定兩個上限：

- **完整討論循環數**：一輪完整的 Pro 計畫／修訂 → Claude Code 交叉檢核 → Agent Teams 討論 → Pro 逐項回應。
- **模型請求數**：每次實際送出的模型請求。重試也算新請求；Agent Teams 中可見的每個代理呼叫分別計數。
- **團隊內部計數限制**：如果 Agent Teams 不揭露個別請求或無法強制上限，記錄實際可見的聚合數和限制；不可宣稱精確控制了未顯示的內部呼叫。
- **完整結果**：每個必要階段都收到實質、可讀且使用正確材料版本的回覆。中斷、空回覆或缺少某角色時，該輪標 INCOMPLETE 或 BLOCKED。

循環數和請求數不可混用。達上限時停止追加呼叫；未解決項目保留為待決。

## 5. 一個審查循環的步驟

1. **Pro 建立或修訂計畫**：列出步驟、依據、前提、風險、未知項和驗收方法。
2. **Claude Code 交叉檢核**：使用相同凍結材料，指出錯誤假設、遺漏、反例、依據不足和失效情境。
3. **Agent Teams 討論**：先保留可辨識的個別觀點，再整理團隊結論；區分支持、反對、補充和未能判定的項目。
4. **Luna 整理並轉回 Pro**：將兩組意見去重、編號，附回原主張與材料版本。Luna 傳遞和整理意見，不把自己當成第三位權威審查者。
5. **Pro 逐項回應**：對每個問題標示採納、部分採納、拒絕或未知，給出理由並提交修訂計畫。
6. **循環關閉檢查**：確認四階段都完整、請求數未超限、驗收條件可檢查。否則標成 INCOMPLETE 或 BLOCKED。

未解決事項若仍在預設上限內，可開始下一循環，沿用最新凍結計畫和未解決清單；材料變更時記錄新版本。達到上限仍有問題，就帶著問題回報，不可把沉默、同意或多數意見當成已排除錯誤。

## 6. 計畫結論與執行關卡

Luna 整理最後版本、審查結果、採納／拒絕理由、未解決意見、驗收狀態和證據位置。結論只能使用以下意義：

- **READY_FOR_DECISION**：檢核清單通過，交由使用者決定是否執行。
- **NEEDS_REVISION**：仍有可修正項目。
- **BLOCKED**：材料、工具、授權或重要依據不足。
- **UNRESOLVED**：達上限但仍有分歧或未知項。

通過檢核不代表零錯誤。若要執行，必須有涵蓋該動作的明確授權。執行前保存基線；執行後逐項標 PASS、FAIL、BLOCKED 或 NOT_ATTEMPTED，附上可讀回的驗收證據。

## 7. 公開前安全檢查

人工審查正文、檔案清單、附件、連結和 Git metadata，確認沒有私人對話、帳號資料、個人路徑或未授權內容。用可信的本機秘密掃描工具檢查目前檔案與完整 Git 歷史，逐項審閱命中；無命中不代表絕對安全。

若掃描器會主動驗證憑證，先確認網路目的地和請求行為；未授權時不執行，也不把私人材料上傳到外部掃描服務。若發現可能已公開的有效憑證，先撤銷或輪替，再清理歷史並重新掃描。確認 repo 可見性和實際待推送內容後才發布。

## 8. 狀態

| 狀態 | 意義 |
| --- | --- |
| DRAFT | 任務範圍尚未凍結。 |
| PREPARED | 需求、材料、上限和驗收條件已記錄。 |
| PREFLIGHT_PASSED | 工具、模型標籤、登入狀態與材料可用性已檢查。 |
| REVIEWING | 在預設上限內審查中。 |
| INCOMPLETE | 有必要階段沒有得到完整可讀回覆。 |
| NEEDS_REVISION | 發現待修正問題。 |
| READY_FOR_DECISION | 指定檢核已完成，等待使用者決定。 |
| EXECUTION_AUTHORIZED | 既有明確授權涵蓋執行範圍。 |
| EXECUTING | 執行已開始但尚未完成驗收。 |
| VERIFIED | 所有必要驗收條件均有證據通過。 |
| PARTIAL | 部分完成，仍有必要項目未完成或未驗證。 |
| BLOCKED／UNRESOLVED | 必要條件不足，或上限到達時仍有未知／分歧。 |
| CANCELLED | 決策者取消後續工作。 |
